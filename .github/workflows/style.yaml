---
name: Style 🎽

on:
  push:
    branches:
      - main
      - pre-release
  pull_request:
    branches:
      - main
      - pre-release
  workflow_call:

jobs:
  style:
    name: Check code style 🔠
    runs-on: ubuntu-latest
    if: "! contains(github.event.commits[0].message, '[skip style]')"
    container:
      image: ghcr.io/insightsengineering/rstudio_4.1.2_bioc_3.14:latest

    steps:
      - name: Checkout Code 🛎
        uses: actions/checkout@v2
        with:
          path: ${{ github.event.repository.name }}

      - name: Install styler 👚
        run: |
          if (!require("styler")) install.packages("styler", repos="http://cran.rstudio.com/")
        shell: Rscript {0}

      - name: Run styler 👟
        run: styler::style_pkg("${{ github.event.repository.name }}")
        shell: Rscript {0}

      - name: Catch unstyled files 👽
        run: |
          cd ${{ github.event.repository.name }}
          git status -s
          if [[ -n `git status -s | grep man` ]]
          then {
            echo "🙈 Code style errors found!"
            echo "🔀 The following differences were noted:"
            git diff man/*
            echo -e "\n💻 Please rerun the following command on your workstation and push your changes"
            echo "--------------------------------------------------------------------"
            echo "styler::style_pkg()"
            echo "--------------------------------------------------------------------"
            echo "ℹ styler version that was used in this workflow: $(R --slave -e "cat(paste(packageVersion('styler')))")"
            exit 1
          } else {
            echo "💚 Code style is up-to-date"
          }
          fi
        shell: bash
