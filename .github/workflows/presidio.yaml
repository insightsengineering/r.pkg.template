---
name: PII Check 💳

on:
  push:
    branches:
      - main
      - pre-release
  pull_request:
    branches:
      - main
      - pre-release

jobs:
  presidio:
    name: PII Check 💳
    runs-on: ubuntu-latest
    if: "! contains(github.event.commits[0].message, '[skip pii]')"
    steps:
      - name: Checkout repo 🛎
        uses: actions/checkout@v2

      - name: Run Presidio ☔
        uses: insightsengineering/presidio-action@beutify-msg
        with:
          output: "standard"
